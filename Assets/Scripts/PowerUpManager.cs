using UnityEngine;

public class PowerUpManager : MonoBehaviour
{
    /*
    public GameObject LifeIncreasePrfb;
    public GameObject PaddleSizeIncreasePrfb;
    public GameObject PaddleSizeDecreasePrfb;
    public GameObject BallSizeIncreasePrfb;
    public GameObject BallSizeDecreasePrfb;


    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    public void ActivatePowerUp(GameObject prefab)
    {
        // Referansý alýnan prefab'in tag'ini kontrol ediyoruz
        switch (prefab.tag)
        {
            case "LifePowUp":
                Debug.Log("LifeIncrease power-up activated.");
                prefab.GetComponent<LifeIncreasePowerUp>()?.ApplyPowerUp();
                break;

            case "PaddleDec":
                Debug.Log("PaddleSizeDecrease power-up activated.");
                prefab.GetComponent<PaddleSizeDecreasePowerUp>()?.Apply();
                break;

            case "PaddleInc":
                Debug.Log("PaddleSizeIncrease power-up activated.");
                prefab.GetComponent<PaddleSizeIncreasePowerUp>()?.ApplyInc();
                break;

            case "BallInc":
                Debug.Log("BallSizeIncrease power-up activated.");
                prefab.GetComponent<BallSizeIncreasePowerUp>()?.ApplyPowerUpBall();
                break;

            case "BallDec":
                Debug.Log("BallSizeDecrease power-up activated.");
                prefab.GetComponent<BallSizeDecreasePowerUp>()?.ApplyPowerUpDec();
                break;

            default:
                // Geçersiz prefab
                Debug.LogWarning("Unknown prefab provided.");
                break;
        }
    }


    */
}
